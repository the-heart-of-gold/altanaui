<!DOCTYPE html>
<html lang="en">
<head>
    <title>Altana Company</title>
    <link rel="stylesheet" href="${request.static_url('altanaui:static/dark.css') }"/>
</head>
<body>

<div>
    <a tal:condition="view.logged_in is None"
            href="${request.application_url}/login">Log In</a>
    <a tal:condition="view.logged_in is not None"
            href="${request.application_url}/logout">Logout</a>
</div>

<h1>${company_name}</h1>
<p>A page all about <a  href="${request.route_url('company', altana_canon_id=altana_canon_id)}">${company_name}</a></p>
<p>Number of records: ${company_context.number_records}</p>

<div>
    <h2>Data Sources</h2>
    <table tal:condition="data_sources">
        <tr tal:repeat="item data_sources">
            <td>${item}</td>
        </tr>
    </table>

    <h2>Restrictions</h2>
    <table>
        <tr tal:repeat="item restrictions">
            <td>${item}</td>
        </tr>
    </table>

    <h2>Risks</h2>
    <table>
        <tr><th>Risk Score</th><th>Risk Type</th></tr>
        <tr tal:repeat="row risks">
            <td>${row.risk_score}</td>
            <td>${row.risk_type}</td>
        </tr>
    </table>

    <h2>Buyers</h2>
    <table>
        <tr tal:repeat="row company_context.buyers">
            <td><a href="${request.route_url('company', altana_canon_id=row.altana_canon_id)}">${row.company_name}</a></td>
        </tr>
    </table>

    <h2>Countries of Destination</h2>
    <table>
        <tr><th>ISO-2 Code</th><th>Name</th><th>Official Name</th></tr>
        <tr tal:repeat="row company_context.countries_of_destination">
            <td>${row.alpha_2}</td>
            <td>${row.name}</td>
            <td>${row.official_name}</td>
        </tr>
    </table>

    <h2>Countries of Operation</h2>
    <table>
        <tr><th>ISO-2 Code</th><th>Name</th><th>Official Name</th></tr>
        <tr tal:repeat="row company_context.countries_of_operation">
            <td>${row.alpha_2}</td>
            <td>${row.name}</td>
            <td>${row.official_name}</td>
        </tr>
    </table>

    <h2>Countries of Origin</h2>
    <table>
        <tr><th>ISO-2 Code</th><th>Name</th><th>Official Name</th></tr>
        <tr tal:repeat="row company_context.countries_of_origin">
            <td>${row.alpha_2}</td>
            <td>${row.name}</td>
            <td>${row.official_name}</td>
        </tr>
    </table>

    <h2>HS Traded</h2>
    <table>
        <tr tal:repeat="item company_context.hs_traded">
            <td>${item}</td>
        </tr>
    </table>

    <h2>Industries</h2>
    <table>
        <tr tal:repeat="item company_context.industries">
            <td>${item}</td>
        </tr>
    </table>

    <h2>Products Received</h2>
    <table>
        <tr tal:repeat="row company_context.products_received">
            <td><a href='/product/id/${row.product_id}'>${row.product_name}</a></td>
        </tr>
    </table>

    <h2>Products Sent</h2>
    <table>
        <tr tal:repeat="row company_context.products_sent">
            <td><a href='/product/id/${row.product_id}'>${row.product_name}</a></td>
        </tr>
    </table>

    <h2>Suppliers</h2>
    <table>
        <tr tal:repeat="row company_context.suppliers">
            <td><a href="${request.route_url('company', altana_canon_id=row.altana_canon_id)}">${row.company_name}</a></td>
        </tr>
    </table>

    <h2>Trading Partners</h2>
    <table>
        <tr tal:repeat="row company_context.trading_partners">
            <td><a href="${request.route_url('company', altana_canon_id=row.altana_canon_id)}">${row.company_name}</a></td>
        </tr>
    </table>

</div>

</body>
</html>